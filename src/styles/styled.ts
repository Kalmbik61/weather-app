export interface IVariables {
  colors: {
    grey: string;
    dark_grey: string;
  };
  backgrounds: {
    primary: string;
    dark: string;
    purple: string;
    light_purple: string;
  };
  linear: {
    primary: string[];
    secondary: string[];
  };
}
